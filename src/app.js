import { helperFunc } from "./utilities/helper";
import { sayBye } from "./utilities/sayBye";

export const mainConcept = () => {
  console.log("Main App has  " + helperFunc + " " + sayBye);
  console.log("Prove the hash code of the bundle will need to update");
};
